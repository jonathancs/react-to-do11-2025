import Task from './components/Task'

function Tasks() {
    return (
        <div>
            {/* tasks.map (task) => ( <Task /> ) */}
            {/* criar um componente de task */}
        </div>
    )
}